namespace :pathways do
  namespace :dialogue_report do
    task :user_types do
      # for each stat in (num users, sessions, pageviews, average time/session, average time/pageview)

        # rows are division/type combos
        # plus type combos

    end
  end
end
