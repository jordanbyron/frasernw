
namespace :pathways do
  task :dialogue_report => :environment do
    # user types table
    # broken down by division:
    # for each division/type combo: num users, sessions, pageviews, average time/session, average time/pageview,
    # + totals

    # paths table
    # broken down by division + user category
    # users, page views
  end
end
