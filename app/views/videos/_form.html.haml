= simple_form_for video do |f|
  = f.label :link, "Video embed link"
  = f.input :link, label: false

  = f.label :title, "Title"
  = f.input :title, label: false

  .form-actions
    = f.button :submit, class: "btn btn-primary"
    = link_to 'Cancel', video, class: "btn btn-danger"
