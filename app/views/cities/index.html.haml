%h1 Cities

%table
  %tr
    %th Name
    %th Province
    %th
    %th
    %th

  - @cities.each do |city|
    %tr
      %td= "#{city.name}"
      %td= "#{city.province}"
      %td= link_to 'Show', city
      %td= link_to 'Edit', edit_city_path(city)
      %td= link_to 'Delete', city, :confirm => "Delete #{city.name}?", :method => :delete

%br

= link_to 'New City', new_city_path
