%h2 Shared Care Items

%table.table.table-condensed.table-striped
  %tr
    %th Name
    %th.admin

  - @sc_items.each do |sc_item|
    %tr
      %td= link_to sc_item.name, sc_item, :class => 'ajax'
      %td.admin.btn-group
        = link_to("<i class='icon-pencil'></i>".html_safe + " Edit", edit_sc_item_path(sc_item), :class => "btn btn-mini ajax")
        = link_to("<i class='icon-trash'></i>".html_safe + " Delete", sc_item, :confirm => "Delete #{sc_item.name}?", :method => :delete, :class => "btn btn-mini")

%p.admin= link_to("<i class='icon-plus-sign'></i>".html_safe + " New Shared Care Item", new_sc_item_path, :class => "btn ajax")
