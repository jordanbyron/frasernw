#section_status

  %hr

  %h3 Status
  = f.input :status_mask,
    as: :select,
    collection: Clinic::STATUS_HASH,
    label_method: :last,
    value_method: :first
  #availability.no-print
    = f.input :unavailable_from,
      label: false,
      wrapper: false,
      input_html: { class: "unavailable_from",
      style: "display:none" }
    :javascript
      $("#specialist_status_mask").each( status_changed );
  = f.input :status_details
  = f.input :limitations
  = f.association :languages, as: :check_boxes, item_wrapper_class: 'inline'
  = f.input :interpreter_available
