- set_page_specific_title "#{@procedure.name}"

.tabbable
  #react_root--template

:javascript
  window.pathways = window.pathways || {};
  window.pathways.dataForReact = #{Oj.dump(@init_data)};

.content-wrapper
  %p.admin.btn-group
    - if can? :update, @procedure
      = link_to("<i class='icon-pencil'></i>".html_safe + " Edit Specialty", edit_specialization_path(@procedure), :class => "btn ajax")
    - if can? :destroy, @procedure
      = link_to("<i class='icon-trash'></i>".html_safe + " Delete Specialty", @procedure, :data => { :confirm => "Delete #{@procedure.name}?" }, :method => :delete, :class => "btn")
