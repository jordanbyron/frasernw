%h2 Specialities

%p.admin= link_to "New Speciality", new_specialization_path, :class => "btn btn-primary"

%table.table.table-condensed
  - Specialization.all.each do |specialization|
    %tr
      %td= link_to specialization.name, specialization
      %td.admin= link_to "<i class='icon-pencil'></i>".html_safe, edit_specialization_path(specialization)
      %td.admin= link_to "<i class='icon-trash'></i>".html_safe, specialization, :confirm => "Delete #{specialization.name}?", :method => :delete
