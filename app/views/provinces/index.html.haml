%h1 Provinces

%table.table.table-striped.table-condensed
  %tr
    %th Name
    %th Abbreviation
    %th Symbol
    %th
    %th
    %th

  - @provinces.each do |province|
    %tr
      %td= province.name
      %td= province.abbreviation
      %td= province.symbol
      %td= link_to 'Show', province
      %td= link_to 'Edit', edit_province_path(province)
      %td= link_to 'Delete', province, :confirm => "Delete #{province.name}?", :method => :delete

%br

= link_to 'New Province', new_province_path
