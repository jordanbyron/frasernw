= simple_nested_form_for @province, html: { class: "form-horizontal" } do |f|
  - if not f.error_messages.blank?
    .alert.alert-error= f.error_messages

  = f.input :name
  = f.input :abbreviation
  = f.input :symbol

  .form-actions
    = f.button :submit, class: "btn btn-primary"
    = link_to 'Cancel', provinces_path, class: "btn btn-danger"
