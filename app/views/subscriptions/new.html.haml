%h1 Create New Update Notification
%p 
  <strong>Update Notifications</strong> have two types:  <strong>News Updates</strong> and <strong>Resource Updates</strong>, which are sent regularly to your email address when you create one below.  By default, pathways administrators do not receive updates unless they create an update notification below.  You can create as many update notifications as you want, customizing them to your needs.

= render 'form'

= link_to 'Back', subscriptions_path
