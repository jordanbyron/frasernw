.content-wrapper
  %h1 My Notifications
  .well
    - if !@subscription.nil? ||  @subscription.present?
      %p#notice= notice
      = @subscription.id
      = @subscription.classification
      = link_to 'Edit', edit_subscription_path(@subscription)
      \|
      = link_to 'Back', subscriptions_path
    - else
      No subscriptions to show
      = link_to 'Go Back', subscriptions_path
